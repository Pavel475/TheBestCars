import PopupWithForm from "./PopupWithForm.js";

export default class PopupWithFormSignUp extends PopupWithForm {
    constructor({popupSelector, submit}) {
        super({popupSelector, submit});
    }
}